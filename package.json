{
  "name": "toolbox",
  "private": true,
  "scripts": {
    "build": "lerna exec -- rollup -c=../../rollup.config.js",
    "watch": "lerna exec -- rollup -w -c=../../rollup.config.js"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@rollup/plugin-commonjs": "^17.1.0",
    "@rollup/plugin-node-resolve": "^11.1.1",
    "@rollup/plugin-typescript": "^8.1.1",
    "lerna": "^3.22.1",
    "rollup": "^2.38.4",
    "typescript": "^4.1.3"
  }
}
